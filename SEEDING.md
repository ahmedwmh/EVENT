# 🌱 Seeding Admin Users

## تثبيت الحزم المطلوبة

```bash
npm install
```

## تشغيل Seed

```bash
npm run db:seed
```

أو مباشرة:

```bash
npx tsx prisma/seed.ts
```

## بيانات الدخول الافتراضية

بعد تشغيل seed، يمكنك تسجيل الدخول باستخدام:

### 👤 Admin 1:
- **Email:** `admin@event.com`
- **Password:** `admin123`

### 👤 Admin 2:
- **Email:** `admin2@event.com`
- **Password:** `admin456`

## ملاحظات

- يتم hash كلمات المرور تلقائياً باستخدام bcrypt
- يمكنك إنشاء حسابات إدارية جديدة من خلال seed script
- في الإنتاج، غيّر كلمات المرور الافتراضية

## تحديث Seed

لإضافة أو تحديث حسابات admin، عدّل ملف `prisma/seed.ts` وشغّل:

```bash
npm run db:seed
```

